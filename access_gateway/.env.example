# Example environment variables for access gateway
PORT=4000
UPSTREAM_URL=http://localhost:3000
LOCK_ENDPOINT_PATTERNS=/lock,/unlock,/teams/:id/lock
# Path to Firebase service account JSON (optional if using ADC)
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/serviceAccount.json
# or set GOOGLE_APPLICATION_CREDENTIALS to the JSON path
